version: v1.1.0
steps:
  - id: build-hello-world
    build: -t $Registry/hello-world -f Dockerfile .
    env:
      - PARENT_IMAGE=mcr.microsoft.com/windows/nanoserver
      - NODEJS_VERSION=14.15.3
  #- id: push-hello-world
    #push: ["$Registry/$repo/hello-world:$ID"]
